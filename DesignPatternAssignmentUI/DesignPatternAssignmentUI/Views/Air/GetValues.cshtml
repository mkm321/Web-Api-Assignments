
@model List<DesignPatternAssignmentUI.Models.Air>
@{
    ViewBag.Title = "GetValues";
}

<h2 align="center">@ViewBag.name Details </h2>
<br />
<br />
<br />
<hr />
<table class="table table-responsive table-striped table-bordered">
    <thead>
        <tr>
            <th style="background-color: yellow; color: blue">Name</th>
            <th style="background-color: yellow; color: blue">From</th>
            <th style="background-color: yellow; color: blue">To</th>
            <th style="background-color: yellow; color: blue">Class</th>
            <th style="background-color: yellow; color: blue">Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var i in Model)
        {
        <tr>
            <td>@i.AirName</td>
            <td>@i.AirFromCity</td>
            <td>@i.AirToCity</td>
            <td>@i.AirClass</td>
            <td>@i.AirPrice</td>
            @using (Html
        .BeginForm("SendPutRequest", "Air", FormMethod.Post))
            {
                if (i.IsSaved == "true")
                {
                    <td><input class="btn btn-primary" type="submit" value="Save" disabled /></td>
                }
                else
                {
                    <td><input type="hidden" name="Save" value="@i.AirId,Save" /><input class="btn btn-primary" type="submit" value="Save" /></td>
                }

            }
            @using (Html
            .BeginForm("SendPutRequest", "Air", FormMethod.Post))
            {
                if (i.IsBooked == "true")
                {
                    <td><input class="btn btn-primary" type="submit" value="Book" disabled /></td>
                }
                else
                {
                    <td><input type="hidden" name="Save" value="@i.AirId,Book" /><input class="btn btn-primary" type="submit" value="Book" /></td>
                }
            }
        </tr>
        }
    </tbody>
</table>
@Html.ActionLink("Saved Items", "Saved", "Air")
&nbsp; &nbsp; &nbsp;
@Html.ActionLink("Booked Items", "Booked", "Air")

