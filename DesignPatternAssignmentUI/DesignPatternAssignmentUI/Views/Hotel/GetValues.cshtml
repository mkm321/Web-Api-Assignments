
@model List<DesignPatternAssignmentUI.Models.Hotel>
@{
    ViewBag.Title = "GetValues";
}

<h2 align="center">@ViewBag.name Details </h2>
<br />
<br />
<br />
<hr />
<table class="table table-responsive table-striped table-bordered">
    <thead>
        <tr>
            <th style="background-color: yellow; color: blue">Name</th>
            <th style="background-color: yellow; color: blue">City</th>
            <th style="background-color: yellow; color: blue">Available Rooms</th>
            <th style="background-color: yellow; color: blue">Room Type</th>
            <th style="background-color: yellow; color: blue">Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var i in Model)
        {
        <tr>
            <td>@i.HotelName</td>
            <td>@i.HotelCity</td>
            <td>@i.HotelAvailableRooms</td>
            <td>@i.HotelRoomType</td>
            <td>@i.HotelPrice</td>
            @using (Html
        .BeginForm("SendPutRequest", "Hotel", FormMethod.Post))
            {
                if (i.IsSaved == "true")
                {
                    <td><input class="btn btn-primary" type="submit" value="Save" disabled /></td>
                }
                else
                {
                    <td><input type="hidden" name="Save" value="@i.HotelId,Save" /><input class="btn btn-primary" type="submit" value="Save" /></td>
                }

            }
            @using (Html
            .BeginForm("SendPutRequest", "Hotel", FormMethod.Post))
            {
                if (i.IsBooked == "true")
                {
                    <td><input class="btn btn-primary" type="submit" value="Book" disabled /></td>
                }
                else
                {
                    <td><input type="hidden" name="Save" value="@i.HotelId,Book" /><input class="btn btn-primary" type="submit" value="Book" /></td>
                }
            }
        </tr>
        }
    </tbody>
</table>
@Html.ActionLink("Saved Items", "Saved", "Hotel")
&nbsp; &nbsp; &nbsp;
@Html.ActionLink("Booked Items", "Booked", "Hotel")
